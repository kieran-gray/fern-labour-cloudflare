name: Deploy Marketing to Cloudflare Pages

on:
  workflow_run:
    workflows: ["Marketing Web CI"]
    types:
      - completed
    branches: ['main']
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    permissions:
      contents: read
      deployments: write
      actions: read
    name: Deploy to Cloudflare Pages
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: marketing-web-build-${{ github.event.workflow_run.head_sha }}
          path: ./marketing/out
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Publish to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./marketing/out --project-name=${{ vars.CLOUDFLARE_PAGES_MARKETING_PROJECT }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}