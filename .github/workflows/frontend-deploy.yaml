name: Deploy Labour Web app to Cloudflare Pages

on:
  workflow_run:
    workflows: ["Labour Web CI"]
    types:
      - completed
    branches: ['main']
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    permissions:
      contents: read
      deployments: write
      actions: read
    name: Deploy to Cloudflare Pages
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: labour-web-build-${{ github.event.workflow_run.head_sha }}
          path: ./apps/labour/web/dist
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Publish to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./apps/labour/web/dist --project-name=${{ vars.CLOUDFLARE_PAGES_LABOUR_APP_PROJECT }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}